<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('./partials/styles') %>
		<link rel="stylesheet" href="/common/css/fab.css" />
		<title> <%=title%> </title>
	</head>

	<body>
		<%- include('./partials/nav') %>
		<section class="content">
			<div class="container">
				<div class="row">
					<div class="col-sm-12">
						<h2>Welcome <%=user %> </h2>
					</div>
				</div>

				<div class="row">
					<div class="col-sm-12">
						<h3>List of all blogs</h3>
						<%if (locals.posts) { %> <% for(let [index,data] of posts.entries()
						|| []){ %>Error : <%=data%> <%} %> <% } %>
					</div>
				</div>
			</div>
			<a
				href="/posts/add/"
				class="float d-flex align-items-center justify-content-center undecorated-link"
			>
				<i class="fa-solid fa-plus fa-2x"></i>
			</a>
		</section>
		<%- include('./partials/footer') %>
	</body>
</html>
